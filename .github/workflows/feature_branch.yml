run-name: Deploy to dev
name: "${{ github.head_ref }}"

on:
  pull_request:
    branches-ignore:
      - main

jobs:
  deploy:
    name: Deploy
    environment:
      name: dev-deploy
    runs-on: ubuntu-latest
    steps:
      - name: Hold Deployment
        run: echo "Hold for Deployment"
      - name: Deploy
        run: echo "Call deployment here test"